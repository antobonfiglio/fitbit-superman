# QA Run Record - 2026-04-03

## Run Metadata
- Date: 2026-04-03
- Scope: First-face validation readiness (superman-classic)
- Build target set: atlas, vulcan
- Branch state: runtime variant implementation + automated layout validator

## Automated Evidence

### Gate Execution
Command:
- npm run qa:gate

Result:
- PASS
- Build completed for app, companion, and settings.
- Layout validation passed for all face variants and targets.

Captured build id:
- 0x03b1594ea5bb7a21

Notes:
- Non-blocking warning observed: Node deprecation warning for fs.Stats constructor.
- Layout validator emitted informational warnings for minimal face weather-disabled mode.

### Layout-Only Execution
Command:
- npm run validate:layout

Result:
- PASS

## Acceptance Status Snapshot

### P0 Criteria
1. Build success: PASS (automated — build ID 0x03b1594ea5bb7a21)
2. Default startup behavior (superman-classic): PASS — face rendered correctly on first install with no crash.
3. Face selection roundtrip: PENDING (manual)
4. Weather toggle behavior: PENDING (manual)
5. Activity toggle behavior: PENDING (manual)
6. Classic face behavior: PASS — time, date, weather text, and all three activity rings visible and active.

### P1 Criteria
1. Color override precedence: PENDING (manual)
2. Typography/layout application: PENDING (manual)
3. Invalid face fallback: PENDING (manual)
4. Weather icon mapping: PENDING (manual)

## Manual E2E Script (Execute in Order)
1. ~~Install and launch app in simulator and device.~~ DONE — screenshot captured.
2. ~~Confirm default startup renders superman-classic correctly.~~ DONE — PASS.
3. Switch to superman-minimal in settings, verify weather and activity are hidden.
4. Switch back to superman-classic, verify date/weather/activity render and update.
5. Apply distinct time/text colors and verify override precedence.
6. Force invalid faceid and verify fallback to superman-classic.
7. Capture screenshots required by evidence checklist.

## Screenshot Evidence
- classic-default-2026-04-03.png: ATTACHED (shows Fri 3 Apr, 11:41, 22°C, three activity rings rendering correctly)

## Defect Log

### DEF-001 — Distance value truncated (FIXED)
- Severity: P2 (minor visual)
- Observed: Distance ring label showed "4." — last character clipped by 40px stat container.
- Root cause: `.toFixed(2)` produced 4-character string (e.g. "4.00") which overflows the centered text area.
- Fix applied: Changed to `.toFixed(1)` in app/index.js — reduces to 3 characters which fits container.
- Status: FIXED

### DEF-002 — Weather icon not visible in simulator
- Severity: P2 (cosmetic, expected in simulator context)
- Observed: `imgWeatherIcon` rendered with empty href — no icon shown next to temperature.
- Root cause: Simulator companion does not return a live weather condition code, so icon mapping is never triggered on first launch.
- Workaround: Weather icon renders correctly on device with live weather data.
- Status: KNOWN / EXPECTED in simulator

## Evidence To Attach
1. Terminal output for qa:gate run — CAPTURED above.
2. Terminal output for validate:layout run — CAPTURED above.
3. Screenshot classic default — ATTACHED.
4. Screenshot minimal variant — PENDING.
5. Screenshot classic after switch-back — PENDING.
6. Screenshot color override — PENDING.
7. Final sign-off decision — PENDING.

## Current Decision
- E2E Flow 1 complete. P0.1 and P0.6 confirmed PASS.
- One P2 defect found and fixed (DEF-001).
- One known simulator limitation noted (DEF-002).
- Remaining manual steps: E2E Flows 2, 3, 4.
